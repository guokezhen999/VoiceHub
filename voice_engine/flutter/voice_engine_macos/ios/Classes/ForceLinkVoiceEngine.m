// Force-link file is not needed when using dynamic frameworks.
// voice_engine.xcframework is a dynamic framework, so CocoaPods links it
// automatically. This file exists only so the `Classes/**/*` source glob in
// the podspec resolves.
