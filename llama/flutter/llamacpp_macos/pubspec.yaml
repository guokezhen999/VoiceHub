name: llamacpp_macos
description: Flutter FFI plugin bundling libllamacpp_nmt.dylib for LLM-based translation.
version: 1.0.0

environment:
  sdk: ^3.0.0
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      macos:
        ffiPlugin: true
